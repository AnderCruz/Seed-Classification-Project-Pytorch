# Seed-Classification-Project-Pytorch
This project implements a neural network using PyTorch to classify seeds based on their physical characteristics. The code is written in a Jupyter Notebook (sementes.ipynb) and demonstrates data preprocessing, model training, and evaluation.
